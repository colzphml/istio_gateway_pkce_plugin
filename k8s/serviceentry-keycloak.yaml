apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: ext-keycloak
  namespace: istio-system
spec:
  hosts:
    - keycloak.example.com
  location: MESH_EXTERNAL
  resolution: DNS
  ports:
    - number: 443
      name: https
      protocol: HTTPS